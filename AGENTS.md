# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Do not leave services running

Do not leave dev servers, watchers, or other long-running services running after a prompt/response is finished.

If a task requires a long-running server, show the user the exact command needed and ask them to run it manually instead of starting it yourself.
